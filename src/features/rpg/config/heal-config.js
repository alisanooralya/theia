/**
 * RPG 2.0 — Heal config. Single source of truth for `.heal` pricing.
 * Rebuilt simple (not migrated): full heal at a flat coin-per-HP rate.
 */
export const HEAL_CONFIG = Object.freeze({
  coinPerHp: 1,
});
